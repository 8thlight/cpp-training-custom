Templates
---------

These examples are focussed on the syntax for
defining and using templates.

Function templates
------------------

function_template.cpp contains a definition of
a generic max function


Class templates
---------------

class_template.cpp contains a definition of a
generic Pair class

N.b. the standard library provides a std::pair
     class so normally wouldn't have to roll our
     own


Non-type templates
------------------

non_type_template.cpp contains an example of template
parameter that is not a type

N.b. the standard library also includes a std::array
     class
